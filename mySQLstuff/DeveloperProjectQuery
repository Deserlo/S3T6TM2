
SELECT P.ProjName, T.taskName, T.duration 
FROM Project P, Task T, Works_on W
WHERE  P.ProjNo = W.ProjNo and W.devID = T.devID 
and T.devID = ?  and T.duration is not null 
GROUP BY P.ProjName 
HAVING MAX(T.end);
